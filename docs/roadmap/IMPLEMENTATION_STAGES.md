# Moved

This workflow implementation document has moved to:

- `docs/workflows/RECAP_END_WORKFLOW_IMPLEMENTATION_STAGES.md`

Keep this file only as a compatibility redirect to avoid breaking older links.
